# P1 업데이트 기록 — v1.2.0

## 1. 불변 데이터 엔진과 배치 발행

- `Quote`, 세션, spark, OHLCV 봉과 반환 배열을 불변 스냅숏으로 전환
- 기존 제자리 객체 변경을 제거하고 변경 심볼만 새 참조로 교체
- 한 엔진 틱에서 여러 전역 콜백을 반복하던 구조를 하나의 `MarketBatch` 발행으로 통합
- 변경되지 않은 자산 참조를 유지해 React 메모이제이션 계약을 명확화
- 캐시된 모든 히스토리 범위의 마지막 봉을 현재 암호화폐 모의 가격과 동기화
- 브라우저가 백그라운드 상태일 때 모의 틱 정지

## 2. 시장 세션 분리

- 정규장, 24/7 연속 세션, 향후 시간외 세션을 `Quote.sessions`로 분리
- 미국 주식·지수·선물은 `regular / closed`
- 암호화폐는 `continuous / open`
- 종목 상세의 시가·고가·저가·거래량·상태·기준 시각을 활성 세션 기준으로 표시
- 정규장 값과 24/7 값을 하나의 필드로 오인하지 않도록 타입 계약 추가

## 3. 브라우저 저장소 안정화

- 관심목록·테마·가격 알림에 런타임 검증과 손상 JSON 복구 추가
- 알 수 없는 심볼, 중복값, 비정상 타입·수치·날짜 제거
- 저장소 접근 거부·quota 오류 시 메모리 상태로 안전하게 계속 동작
- 관심목록·테마·알림을 `storage` 이벤트로 여러 탭 간 동기화

## 4. 가격 알림 정확성

- `가격 >= 목표` 같은 단순 상태 확인을 실제 방향성 임계값 교차 감지로 교체
- 알림 생성·외부 탭 수신 시 현재 가격을 기준선으로 저장해 즉시 오작동 방지
- 한 알림은 최초 교차 시에만 트리거되고 새 알림 배지와 토스트를 생성
- 저장 알림 개수와 필드 범위를 제한

## 5. 키보드·스크린리더 접근성

- 관심목록·암호화폐·스크리너·공통 시세 행의 종목 이동을 실제 링크로 변경
- 스크리너 정렬을 `<th>` 클릭에서 `<button>` + `aria-sort` 구조로 변경
- 검색을 combobox/listbox/option 패턴으로 재작성하고 화살표·Enter·Escape 지원
- 관심목록 추가 검색에도 동일한 키보드 선택 패턴 적용
- 히트맵 Canvas에 모든 종목을 제공하는 접이식 대체 데이터 표 추가
- 데이터 테이블에 caption, scope, 이름 있는 scroll region 추가
- skip link와 경로 전환 시 본문 포커스 이동 추가

## 6. 모달·포커스 관리

- 공유 `Modal` primitive를 Portal 기반으로 추가
- 초기 포커스, Tab 순환, Escape 닫기, body 스크롤 잠금 구현
- 앱 root를 `inert`/`aria-hidden`으로 격리하고 닫을 때 원래 호출 요소로 포커스 복귀
- 검색, 히트맵 확대, AI 패널, 알림 생성 화면에 공통 모달 계약 적용
- 모달이 열린 상태에서 전역 검색 모달이 중첩되지 않도록 방지

## 7. 모바일·감소된 모션

- 헤더를 1000px, 700px, 520px, 360px 구간으로 재설계
- 작은 화면에서 배지·긴 검색 문구·브랜드 텍스트를 단계적으로 축소
- 모바일 검색을 100dvh 전체 화면으로 전환
- 전역 `prefers-reduced-motion` 대응 추가
- 페이지 패딩과 데이터 테이블 스크롤 영역 조정

## 8. 라우팅·오류 처리

- 알 수 없는 경로가 시장 홈으로 보이던 fallback을 실제 404 페이지로 교체
- 시장·암호화폐·실적·예측·스크리너·정치인·관심목록·앱·종목·404별 문서 제목과 meta description 추가
- 경로별 Error Boundary가 운영 화면에 원시 오류 메시지를 노출하지 않고 오류 ID만 표시하도록 수정
- 개발 환경에서만 stack 세부정보 노출

## 9. 테스트·CI

- Vitest, Testing Library, jsdom 도입
- 엔진 불변성·한 틱 한 배치·OHLCV 계약 테스트
- 저장소 정제와 다중 탭 이벤트 테스트
- 알림 방향성 교차와 저장 데이터 정제 테스트
- 검색 combobox 키보드 테스트
- 모달 포커스 트랩·배경 격리·포커스 복귀 테스트
- 경로별 문서 제목·404 메타데이터 테스트
- DOM 없는 독립 계약 스모크 테스트 추가
- GitHub Actions에서 `npm ci` → 전체 검사 → high 의존성 감사 실행
