/* ============================================================
   Bundled brand marks, keyed by symbol.

   Generated by scripts/fetch-logos.mjs from public ticker-logo sources.
   Files live in public/logos/ and are served as static assets, so they
   work offline and no third party learns which tickers a user opens —
   an external logo CDN would leak exactly that.

   Coverage is deliberately partial. A symbol with no entry here falls
   back to the existing coloured initial chip; that path must keep
   working rather than render a broken image.
   ============================================================ */

